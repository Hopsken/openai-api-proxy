{
  "name": "openai-api-proxy",
  "version": "0.1.0",
  "description": "Proxy OpenAI API via Cloudflare Workers for secure, fast, and global access to language processing capabilities.",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230228.0",
    "typescript": "^4.9.5",
    "wrangler": "2.12.0"
  },
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler publish"
  }
}